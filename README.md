# My Little Play Area

Decided to write an OS from scratch - not because I'm weird (I am weird, but it's not the reason), but because I don't know how it works it seems like an interesting challenge

## Running

You'll need nasm, bochs, and make installed - to compile the application run `make`, then run `bochs` to run the application. `make clean` will remove all binary files created during `make`.
